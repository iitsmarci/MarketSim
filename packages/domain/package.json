{
  "name": "@marketsim/domain",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint src --max-warnings=0",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "test": "vitest run",
    "build": "tsc --project tsconfig.build.json"
  },
  "devDependencies": {
    "@testing-library/dom": "^10.4.2"
  }
}
